# 呷啥 v8

Vercel 靜態網站 + `/api/nearby` Serverless Function。

部署設定：
- Framework Preset：Other
- Root Directory：./
- Build Command：留白
- Output Directory：留白
- Install Command：留白

此版本刻意不放 package.json，避免 Vercel 把整個專案誤判成 Node/Express 伺服器。
